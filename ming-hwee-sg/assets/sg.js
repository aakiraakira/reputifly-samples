/* ==========================================================================
   MingHwee SG Landing Page - Page-specific JS
   All core behavior (modal, forms, FAQ accordion, testimonial carousel,
   fade-in on scroll, UTM capture, smooth scroll, CTA tracking) lives in
   shared.js. This file is intentionally light.
   ========================================================================== */
(function () {
  'use strict';

  // No extra page-specific logic needed on SG. Everything is handled by
  // shared.js bindings via data-open-modal, data-preselect-type,
  // data-cta-type, and the standard .faq-item / .testimonial / .fade-in
  // / form[data-form-type] selectors.
})();
